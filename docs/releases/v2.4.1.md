[Español](v2.4.1.es.md)

## Summary

Release automation repair. Runtime behavior is identical to `v2.4.0`.

## Changes

- Published releases immediately so release-please can find the previous tagged version.
- Corrected the missing-baseline problem that generated full-history notes and phantom version proposals.
- Removed the erroneous 2.5.0 release and closed the unmerged 2.6.0 proposal.

## Upgrade

No runtime or configuration migration is required. Installations already running `v2.4.0` receive no playback changes from this patch.

## Verification

Check that release-please uses `v2.4.1` as the baseline and that subsequent notes contain only changes since that tag. Playback checks are the same as for `v2.4.0`.
