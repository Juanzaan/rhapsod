services:
  rhapsod:
    build: .
    image: rhapsod:latest
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      RHAPSOD_ENV_FILE: /app/.env
      RHAPSOD_PANEL_HOST: 127.0.0.1
      RHAPSOD_YTDLP_DAEMON_URL: ${RHAPSOD_YTDLP_DAEMON_URL:-http://127.0.0.1:8765}
    depends_on:
      - ytdlp
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data

  ytdlp:
    build: .
    image: rhapsod:latest
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      RHAPSOD_YTDLP_DAEMON_HOST: 127.0.0.1
    command: ["python3", "scripts/yt-dlp-daemon.py"]
    volumes:
      - ./data:/app/data:ro
